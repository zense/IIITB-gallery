<div class="page-header">
  <%= link_to group_path(@group), class: 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Back
  <% end %>
  <h1>New Admin</h1>
</div>

<%= form_tag({:action => :add_admin},class: "form-horizontal", onsubmit:"before_submit()", id:"mailForm", method: :post, multipart: true)do %>
        <!-- <p id="email_id">Emails: <%= text_field_tag :emails %></p>
        <div><%= submit_tag 'Add' ,class: "btn"%> -->
	  <div class="form-group">
	    <label class="col-sm-2 control-label" for="emails">Emails</label>
	    <div class="col-sm-10">
	      <input class="form-control" type="text" name="emails" id="emails" />
	    </div>
	  </div>
	  <div class="form-group">
	    <div class="col-sm-offset-2 col-sm-10">
	      <input type="submit" name="commit" value="Add Admins" class="btn btn-primary" data-disable-with="Add Admins" />
	    </div>
	  </div>
<% end %>
